# Shared 使用规则

`shared/` 只放四方语义完全一致、需要同版本维护的极少量内容，例如：

- 从 `contracts/` 自动生成的 DTO 或客户端；
- 与业务无关的通用错误包络、追踪标识工具；
- 四方共同确认的基础测试工具。

不要放入：

- 设备、预警、工单等各模块自己的领域实体；
- 某个模块为了调用方便复制出来的数据库模型；
- 只被一个模块使用的工具类；
- 没有明确负责人和测试的“大杂烩”代码。

任何 `shared/` 修改必须由受影响成员评审。公共 DTO 应从契约生成或逐字段核对，禁止多个成员各自维护近似版本。
