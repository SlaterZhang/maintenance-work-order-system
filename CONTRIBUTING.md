# 贡献与协作规范

## 1. 标准工作流

所有任务必须遵循：

`Issue → 从 main 建分支 → 编码与测试 → Draft PR → 自动检查 → 队友评审 → Squash merge → 删除分支`

不得先写大量代码，再口头通知队友接口已经改变。跨模块变化必须从契约开始。

## 2. Issue 规则

每个 Issue 至少包含：

- 需求编号，例如 `C-FR-14`、`C-NFR-04`、`E2E-03`；
- 所属成员与模块；
- 输入、输出、异常和验收条件；
- 是否修改接口、字段、状态、枚举或数据库；
- 完成后需要提供的证据，例如测试结果、截图或示例报文。

一个 Issue 只解决一个明确问题。预计超过 1—2 天的任务，应拆成多个可独立合并的 Issue。

## 3. 分支规则

格式：`<type>/<scope>-<requirement>-<description>`。

| type | 用途 |
| --- | --- |
| `feat` | 新功能 |
| `fix` | 缺陷修复 |
| `refactor` | 不改变外部行为的重构 |
| `test` | 测试与测试数据 |
| `docs` | 文档与模型 |
| `chore` | 工具、配置、依赖 |

`scope` 使用 `a`、`b`、`c`、`d`、`shared`、`contract` 或 `docs`。禁止长期使用个人总分支（如 `zhang-dev`）堆积所有改动。

创建分支前必须同步：

```bash
git switch main
git pull --ff-only
git switch -c feat/c-C-FR-14-spare-query
```

## 4. 提交信息

格式：

```text
<type>(<scope>): <简短说明>
```

示例：

```text
feat(c): 实现 C-FR-14 备件查询
fix(contract): 修正 WarningRaised 风险等级枚举
test(shared): 增加 E2E-03 缺件等待场景
docs(a): 补充设备状态字段说明
ci(d): 增加跨模块契约检查
```

每次提交只包含一个逻辑变化；格式化全仓库、批量改名和功能开发不要混在一个提交中。禁止使用“update”“修改一下”“最终版”等不可追踪说明。

## 5. Pull Request 规则

PR 应满足：

- 标题同样使用 `type(scope): 说明`，因为 Squash merge 后它会成为 `main` 上的提交标题；
- 关联 Issue，例如 `Closes #12`；
- 标出需求编号和受影响模块；
- 描述“为什么改”“改了什么”“如何验证”；
- 勾选接口、数据库、枚举、配置等影响项；
- 附上自动测试、接口示例或界面截图；
- 规模尽量控制在一个评审者 20—30 分钟可审完的范围内。

默认需要一名非作者成员批准。以下情况需要所有受影响成员确认：

- 修改 `contracts/`、`shared/` 或跨模块状态枚举；
- 修改 `EquipmentId`、`WarningId`、`OrderId` 的含义或约束；
- 修改端到端验收条件；
- 引入不向后兼容的接口或数据变更。

成员 D 是公共契约、CI 和端到端联调的守门人，但不能单方面改写 A/B/C 的业务语义；涉及业务字段时仍须相应模块负责人确认。

作者不得自行批准自己的 PR。评审意见未解决、自动检查失败或分支落后导致冲突时不得合并。

## 6. 合并与冲突处理

- 统一使用 **Squash merge**，PR 标题作为最终提交说明。
- 合并前优先把最新 `main` 合入当前分支；禁止对共享分支强制推送。
- 发生冲突时，由当前 PR 作者解决，并请被冲突文件的负责人复查。
- 不得通过删除对方代码、注释掉校验或降低测试标准来“解决”冲突。

## 7. 契约优先规则

跨模块协作按照以下顺序：

1. 新建“契约变更”Issue，写清调用方、提供方和兼容策略；
2. 修改 `contracts/openapi.yaml`、JSON Schema、枚举和示例；
3. 由受影响成员评审并合并契约；
4. 各模块分别实现；
5. 用同一组示例和端到端场景联调。

禁止：直接读取对方数据库表、复制对方实体类后各自修改、同一字段在三处维护三套枚举。

## 8. 完成定义（Definition of Done）

一个任务只有同时满足以下条件才算完成：

- 需求编号和验收条件明确；
- 正常流程与至少一个异常流程已实现；
- 新增或修改的行为有测试；
- 接口、示例、模型和代码一致；
- 无密钥、临时日志、调试开关和个人路径；
- PR 自动检查通过；
- 队友完成评审；
- 合并后 `main` 可构建、可运行或至少通过现有自动测试。

## 9. 安全与数据

- 使用环境变量保存密钥，只提交 `.env.example`。
- 示例使用虚构的设备、用户和告警数据。
- 日志不得记录密码、Token、完整联系方式或敏感工业参数。
- 发现密钥误提交时，不仅要删除文件，还必须立即撤销并轮换该密钥。
